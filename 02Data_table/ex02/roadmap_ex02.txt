🇫🇷 Exercice 02 – Compare mon pays

Dossier à rendre : ex02/
Fichiers à rendre : load_csv.py, aff_pop.py
Bibliothèques autorisées : matplotlib, seaborn ou toute autre bibliothèque de visualisation de données

🎯 Objectif de l’exercice :
Créer un programme qui :
1. Appelle la fonction load() du premier exercice (load_csv.py)
2. Charge le fichier population_total.csv
3. Affiche l’évolution de la population de ton pays et d’un autre pays de ton choix sur le même graphique

📊 Graphique attendu :
- Un titre (ex : Comparaison de la population entre la France et le Japon)
- Une légende pour chaque axe (Année / Population (millions))
- Une légende pour chaque courbe (France / Japon)
- Les années affichées doivent aller de 1800 à 2050

💡 Exemple :
Pour un campus situé en France, le graphique doit montrer la population de la France comparée à celle d’un autre pays (par exemple le Japon).

───────────────────────────────
🧭 Roadmap – Ex02 : Compare my country

1️⃣ Crée la structure du projet
- Dossier : ex02/
- Fichiers : 
  - load_csv.py (copie de celui de l’ex00)
  - aff_pop.py (nouveau fichier)

2️⃣ Prépare ton environnement
- Vérifie que matplotlib ou seaborn est installé :
  pip install matplotlib
  ou
  pip install seaborn

3️⃣ Charger les données
- Importe ta fonction load() depuis load_csv.py
- Charge le fichier population_total.csv
- Vérifie que le DataFrame contient bien les pays et les années

4️⃣ Sélectionner les deux pays
- Filtre les lignes correspondant :
  - à ton pays (ex: France)
  - à un autre pays (ex: Japon)
- Tu dois avoir deux DataFrames (ou deux séries de valeurs)

5️⃣ Extraire les données nécessaires
- Récupère la liste des années (colonnes de 1800 à 2050)
- Récupère les valeurs de population correspondantes pour chaque pays

6️⃣ Tracer les deux courbes
- Utilise plt.plot() pour chaque pays, en changeant la couleur ou le style de ligne
- Ajoute un titre et les légendes d’axes
- Ajoute une légende de courbes (avec plt.legend())

7️⃣ Restreindre les années à 1800–2050
- Si ton dataset va au-delà de 2050, filtre les colonnes avec : "1800":"2050"

8️⃣ Tester ton programme
- Lance python3 aff_pop.py
- Vérifie que le graphique montre bien les deux pays, les bonnes années et les légendes

───────────────────────────────
📘 Cours : notions à comprendre et exemples

1️⃣ Sélectionner plusieurs pays
------------------------------
Pour filtrer deux lignes dans un DataFrame :
>>> df_filtered = df[df["country"].isin(["France", "Japan"])]
Cela garde uniquement les lignes où 'country' vaut France ou Japan.

2️⃣ Restreindre les colonnes (années)
------------------------------------
Pour ne garder que les colonnes de 1800 à 2050 :
>>> df_years = df_filtered.loc[:, "1800":"2050"]

3️⃣ Extraire les valeurs d’un pays
----------------------------------
Tu peux récupérer les années et les valeurs comme ceci :
>>> years = df_years.columns.tolist()
>>> france_values = df_years[df_years["country"] == "France"].values[0][1:]
>>> japan_values = df_years[df_years["country"] == "Japan"].values[0][1:]

4️⃣ Tracer deux courbes sur le même graphique
---------------------------------------------
>>> import matplotlib.pyplot as plt
>>> plt.plot(years, france_values, label="France")
>>> plt.plot(years, japan_values, label="Japon")
>>> plt.title("Comparaison de la population France vs Japon")
>>> plt.xlabel("Année")
>>> plt.ylabel("Population (millions)")
>>> plt.legend()
>>> plt.show()

5️⃣ Astuce : personnaliser les courbes
--------------------------------------
- Changer la couleur : plt.plot(..., color="red")
- Changer le style de ligne : plt.plot(..., linestyle="--")
- Ajouter des marqueurs : plt.plot(..., marker="o")
- Ajouter une grille : plt.grid(True)

───────────────────────────────
💡 En résumé :
- 1 courbe = ton pays
- 1 autre courbe = le pays choisi
- Les deux partagent le même axe X (les années)
- Tu ajoutes titre, légendes et plt.legend() pour les distinguer
